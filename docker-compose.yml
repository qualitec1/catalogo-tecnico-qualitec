version: '3.8'

services:
  qualitec-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: qualitec-catalogo
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
      - PORT=3000
